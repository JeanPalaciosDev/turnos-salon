{
  "name": "@turnos/models",
  "version": "0.0.1",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "dev": "tsc --build --watch",
    "clean": "rimraf dist .turbo"
  },
  "dependencies": {
    "@turnos/core": "*"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "rimraf": "^6.0.0"
  }
}
